# Changelog

本项目遵循 Semantic Versioning。历史制品一经发布即保持不变。

## [0.4.1-alpha.14] - 2026-08-31 (distributed test build)

### Fixed

- 模型输入中的 URL 现在必须引用当前用户拥有的 Halo 附件；服务端会重新读取附件、核验归属并使用 Halo 生成的 permalink，拒绝客户端直接提供的远程地址。
- 图像生成在保存多张结果时遇到失败或取消会回滚尚未提交到会话的附件，避免留下孤立的生成附件。
- 浏览器对 Job 查询的全部 `5xx` 响应视为可恢复的未知状态；本地缓存访问失败会降级而不是中断会话流程。
- 全局设置 ConfigMap 损坏时拒绝新的模型调用；管理端图片大小字段与 10 MiB 服务端硬上限保持一致。
- AI Foundation 的用户可见错误改为稳定、可操作的分类提示，不再直接显示 Provider 原始错误详情。

### Security

- 生成图片的远程下载仅接受经 DNS 校验的公网站点 HTTPS 地址，并在 Base64 解码前后执行大小限制。
- Markdown 与历史图像仅加载同源附件或受限的 `data:image`，避免模型输出诱导浏览器请求第三方跟踪图片。

## [0.4.1-alpha.13] - 2026-08-29 (unreleased development build)

### Changed

- 将 AI Foundation 声明依赖和编译期 SDK 提升至稳定版 `>=1.0.1` / `1.0.1`；不再使用 `*` 依赖范围兼容预发布版本解析限制。
- 启动期版本校验同步提升为 `1.0.1`，缺失、无效或较低版本会拒绝启动。

## [0.4.1-alpha.12] - 2026-08-29 (unreleased development build)

### Changed

- 图片生成结果会由后端下载、限制大小、重新校验并写入 Halo 附件库；会话保存本地附件链接，避免 Provider 临时 URL 过期。

## [0.4.1-alpha.11] - 2026-08-29 (unreleased development build)

### Security

- 统一图片部件的类型、PNG/JPEG MIME 与数量校验，阻止以空或伪造媒体类型绕过图片配额。

## [0.4.1-alpha.10] - 2026-08-24 (unreleased development build)

### Fixed

- 会话快照更新保留服务端已有的创建时间，仅首次创建时写入服务器时间；图片缓存创建时间同样不接受客户端值。

## [0.4.1-alpha.9] - 2026-08-24 (unreleased development build)

### Fixed

- 会话大小以 UTF-8 字节数校验，避免非 ASCII 内容通过持久化检查后又被 HTTP 请求体限制拒绝。
- Data URL 的实际媒体类型必须与声明类型一致，防止媒体类型伪造进入 AI Foundation。
- 图片缓存的创建时间只由服务端写入，客户端时间不再能够延长缓存保留期。

## [0.4.1-alpha.8] - 2026-08-24 (unreleased development build)

### Fixed

- 服务端在反序列化聊天、图像、设置、缓存和快照 JSON 前统一限制请求体为 1 MiB，拒绝空请求体和无效 JSON。
- 审计记录只使用实际连接对端地址，不再信任可由客户端伪造的转发 IP 请求头。
- 每日 token 配额在创建文本、聊天和图像任务时预留服务端估算输入与受控输出预算；取消、重启中断和插件停用会按同一预留额度释放。

### Security

- 追加配额单元测试，覆盖最大输出预算计入每日限额的边界。

## [0.4.1-alpha.7] - 2026-08-23 (unreleased development build)

### Fixed

- 统一代理当前兼容性基线与插件版本，避免测试、打包和发布按过时的 alpha.4 规则核验。
- 上传入口仅接受 PNG 与 JPEG；服务端在完整解码前读取尺寸元数据，并将通过校验的图像重新编码为 PNG，移除原始元数据和尾随内容。
- 图片上传改为客户端顺序上传、服务端最多两路同时处理，避免一次选择多张图片时放大内存与解码压力。
- 个人设置不再接受图片大小上限；该值始终以管理员全局配置和服务端硬上限为准。

### Security

- 单张图片的服务端硬性上限收紧为 10 MiB；图片解析移至受限的弹性工作线程，超出并发上限时返回 429。

## [0.4.1-alpha.6] - 2026-08-17 (unreleased test build)

### Fixed

- 页面卸载、切换或组件停用只会断开本地 SSE/轮询观察，不会将仍在服务端运行的 Job 误写为已取消。
- 轮询在临时网络错误、网关错误或限流时使用上限为 10 秒的退避重试；只有服务端明确返回终态错误才标记回复失败。
- 等待创建的聊天和图像任务、取消意图与重试按钮均按会话隔离，收藏视图操作不会受当前其他会话状态影响。
- 普通聊天角色不再拥有 Halo 原生附件创建权限；图片只能经过插件受限上传入口进入附件库。
- 后端上传入口校验 PNG/JPEG/GIF 文件签名、实际可解码性、宽高和像素上限，拒绝 SVG 与伪造图片类型。

### Compatibility

- Requires Halo `>=2.25.0`.
- Requires AI Foundation `>=1.0.0-beta.5`.
- `0.4.1-alpha.6` 为独立测试构建，不能替换任何已发布或已分发制品。

## [0.4.1-alpha.5] - 2026-08-17 (unreleased test build)

### Fixed

- 收藏状态作为消息字段持久化并在刷新后恢复。
- 重试与编辑后重新生成会在用户确认和 Job 创建成功前保留原消息分支。
- 活动 Job、停止生成按钮及取消请求按会话隔离；组件卸载会关闭本地 SSE/轮询观察连接。
- 长对话摘要和上下文压缩使用分块处理，且跳过图像生成模型。

### Compatibility

- Requires Halo `>=2.25.0`.
- Requires AI Foundation `>=1.0.0-beta.5`.
- `0.4.1-alpha.5` 是未发布测试构建，不能替换任何已分发制品。

## [0.4.1-alpha.4] - 2026-08-10 (unreleased test build)

### Added

- 新增角色模板权限修复：普通用户聊天角色包含 SSE、Job 取消、图片缓存、附件上传和个人数据控制所需资源；审计角色与管理员角色分离。
- 新增服务端 `GET /me/identity` 身份能力结果；前端按当前登录用户名隔离浏览器 localStorage 缓存，身份未确认前禁止读取旧缓存。
- 新增按会话 ID 维护的同步冲突集合、冲突冻结态和可辨识恢复入口；多会话同时冲突时展示标题、会话 ID 和详情，小屏幕下操作按钮单列布局。
- 新增冲突写入守卫：聊天、图像、重试、重命名、删除等 15 项操作在进入业务逻辑前统一拒绝修改冲突会话，相关控件同步禁用。
- 新增会话快照服务端单调版本号（`_version`）和 CAS（乐观锁）语义；冲突时使用基线、本地草稿和服务器快照进行三方合并。
- 新增会话删除标记（保留 30 天），阻止后台 Job 或在途快照重新创建已删除会话；到期由定时任务清理。
- 新增前端 owner 缓存、会话目标和 session-version 模块及对应自动化测试（12 个前端测试，30 个 Java 测试）。
- 新增后端模型请求包络校验、配额判定、Job 生命周期策略、持久化 owner 校验和权威审计记录工厂等可独立测试模块。
- 新增 `GET /jobs/recoverable` 接口，浏览器刷新后以持久化 Job 为事实来源恢复活动任务、消息和锁定状态。
- 新增根 Gradle 构建、Gradle Wrapper（含发行包校验和）、依赖锁、Java 21 工具链和可重复归档配置。
- 新增 GitHub Actions CI（干净检出执行 Java/前端测试与 JAR 打包）、CODEOWNERS 和人工复核清单。

### Changed

- 调用日志改为仅服务端写入；文本生成、标题、摘要和压缩统一进入服务端限流、配额预留和审计路径。
- 模型选择策略使普通用户无需访问 `/global-settings` 即可获取白名单和默认模型；后端额外要求管理权限方可读取全局设置。
- 自动上下文控制不再重写或删除已保存消息；手动摘要写入 session memory 并保留原始消息。
- 附件改用 Halo `AttachmentService` 处理，移除基于请求派生 authority 的内部 HTTP 代理和 Cookie/XSRF 转发。
- 窄屏会话侧栏改为可动画打开的覆盖抽屉（`display: flex` 实现）。
- 代码高亮改为 DOM 节点构造，避免高亮标签属性混入代码文本。
- 摘要、压缩和标题辅助调用仅使用该会话最近一次实际使用且仍可用的语言或多模态模型，不跨模型调用。
- 手动与自动上下文压缩改为上限受控的分块摘要；自动压缩失败显示可见错误。
- 历史迁移在 Halo 缺失旧扩展索引时，界面展示安全迁移不可用的具体原因，不再误报迁移成功。

### Fixed

- 收藏状态现在作为消息字段由后端规范化、持久化并回传；刷新、重启和跨设备会话加载后仍可恢复。
- 重试旧消息时先确认模型回退，再截断会话；若后台 Job 创建失败会恢复原有消息分支，避免取消确认或请求失败删除后续对话。
- 活动 Job 状态改为按会话维护；切换会话时输入区、停止按钮和取消操作只作用于当前会话，多个恢复任务会显示合并的停止入口。
- 聊天组件卸载时主动关闭其 SSE 与轮询观察连接，不会取消服务器后台 Job；重新进入聊天页后仍可通过持久化 Job 恢复。
- 辅助摘要/压缩会从后向前选择最近可用的语言或多模态模型，跳过图像生成模型；对话摘要和上下文压缩均按大小分块，长消息不再静默截断。
- 修复 `0.4.1-alpha.3` 测试制品被同版本重建导致 SHA-256 校验和不对应的问题。
- 修复管理员能力检测由模糊字符串匹配改为精确授权值匹配；未认证请求不再以 `anonymous` 共享身份执行。
- 修复普通用户设置页不必要请求管理员审计接口导致 403 的问题，改用身份能力字段控制入口。
- 修复会话同步竞态：待同步标记按会话 ID 和随机确认 token 保存，每会话拥有独立延迟计时器，同步前后均校验当前 owner。
- 修复 Job 创建覆盖其他标签页已保存内容的问题：Job 创建仅合并新消息，文本和图像 Job 流式/终态写回在每次 CAS 闭包内重新读取当前快照并单调递增版本。
- 修复异步摘要、标题和 Job 回写因使用 `current.value` 在用户切换会话或账号后可能写入错误会话的问题；所有异步写回现在绑定发起时不可变的 owner/session ID。
- 修复摘要成功后引用未定义变量导致误报失败的问题。
- 修复新建会话在首条消息与快照并发时缺少基线导致伪同步冲突的问题；首次持久化建立明确版本零基线，遗留等价 pending 自动采用服务器快照。
- 修复已完成 Job 刷新后仅因元数据差异被误判为编辑冲突的问题；等价比较忽略版本、Token、流式过程和更新时间。
- 修复收藏状态仅在快照写入成功后持久化，失败会回滚。
- 修复停止生成：只在校验 `cancelled` 终态后中止本地 SSE；`409`、网络错误和仍运行状态继续等待。
- 修复身份接口短暂 `401` 不会清空浏览器持久化缓存和待同步数据；仅清空内存视图。
- 修复个人数据删除部分失败时不再清空本地恢复缓存。
- 修复辅助文本生成接口仅接受 `title-generation`、`conversation-summary`、`context-compression` 三个白名单操作。
- 修复 `success` 终态纳入 Job 生命周期和清理策略；成功聊天和图像 Job 的 SSE sink 正常完成和释放。
- 修复已完成 Job 重连时直接发送终态事件；活动 Job 注册 sink 后重新读取权威状态。
- 修复双标签页下 SSE 事件在注册与订阅之间到达的丢失问题。

### Security

- 图像生成请求增加字段白名单，拒绝客户端透传自定义请求头、供应商选项、重试次数和并发度；限制单次生成数量、尺寸、像素数和响应格式。
- AI Foundation 文本流、文本生成和图像生成均增加超时，避免上游无限等待。
- 聊天和图像 Job 写回显式核对持久化 owner；审计记录由后端统一生成并限制字段长度。
- `data:image` 生成图片可进入后续多模态上下文，任意远程 URL 不会自动转交模型。
- `HaloAiConsoleEndpoint` 实现关闭生命周期：释放心跳、恢复调度，停止运行任务并标记为 interrupted，八秒有界窗口持久化。
- 受限附件上传改为流式代理（仍需后续补充文件签名校验）。

### Privacy

- 多次更新 `PRIVACY.md`：补充用户隔离缓存、附件服务托管、任务关闭、上下文保护、离线缓存、身份失败和辅助模型调用的隐私说明。
- 普通用户角色不再读取全局设置，设置页不探测管理员权限。

### Compatibility

- Requires Halo `>=2.25.0`.
- Requires AI Foundation `>=1.0.0-beta.5`.
- `0.4.1-alpha.4` 是未发布的测试构建，不能替换 `0.3.5` 或任何已分发的制品。
- 会话快照 API 要求携带 `_baseVersion`（见 alpha.1 的 Breaking Changes）；`success` 作为新增持久化终态保留 `completed` 兼容读取。

## [0.4.1-alpha.3] - 2026-08-10 (unreleased test build)

### Fixed

- 将 Job 恢复、终态收敛、取消原位更新、重试和收藏的会话归属、窄屏侧栏、附件反馈与代码高亮修复纳入新的唯一测试制品。
- 避免与本机已安装但内容不同的 `0.4.1-alpha.2` 测试 JAR 发生版本冲突。

### Privacy

- 补充离线缓存、身份短暂失败和辅助模型调用的隐私说明；未新增遥测、分析或第三方资源。

### Compatibility

- Requires Halo `>=2.25.0`.
- Requires AI Foundation `>=1.0.0-beta.5`.
- `0.4.1-alpha.3` 是未发布的测试构建，不能替换 `0.3.5` 或任何已分发的制品。该测试构建因同版本重建导致校验和不再对应，保留为历史记录；修复见 `0.4.1-alpha.4`。

## [0.4.1-alpha.2] - 2026-08-06 (unreleased test build)

### Fixed

- 修复新会话刷新后因缺少本地基线而被错误标记为同步冲突的问题：空会话不再抢先创建快照，首次持久化建立版本零基线；服务器与本地内容等价的遗留 pending 自动采用服务器快照。
- 会话等价比较忽略服务器权威的版本、Token、流式状态和更新时间字段，避免已完成 Job 在刷新后仅因元数据差异产生冲突。

### Compatibility

- Requires Halo `>=2.25.0`.
- Requires AI Foundation `>=1.0.0-beta.5`.
- `0.4.1-alpha.2` 是未发布的测试构建；不会替换 `0.3.5` 的历史制品。

## [0.4.1-alpha.1] - 2026-08-06 (unreleased test build)

### Added

- 增加 Gradle Wrapper、依赖锁、前端构建/测试入口和 GitHub Actions CI，使干净检出可用 Java 21 与 Node.js 22 构建和验证。
- 将会话目标隔离、浏览器 owner 缓存、请求上下文限制、配额判定、Job 生命周期、owner 校验和权威审计记录拆分为可独立测试的模块。
- 增加普通角色模板、跨用户缓存隔离、配额、长上下文、Job 终态和审计字段的自动化覆盖，以及发布前人工审核清单。

### Fixed

- `success` 作为现有聊天和图像 Job 的成功终态纳入 Job 生命周期策略，成功 Job 的 SSE sink 可正常完成和释放，保留清理会处理成功记录。
- 已完成 Job 重连时直接发送唯一终态事件并结束；活动 Job 在注册 replay sink 后重新读取权威状态，避免读后注册和订阅前终态事件丢失。
- 修复异步摘要、压缩、标题与 Job 回写在用户切换会话或账号后可能写入错误会话的问题。
- 修复摘要成功后引用未定义会话变量导致误报失败的问题。
- 新建会话在启动聊天或图像 Job 前会先完成版本为零的快照保存；Job 事件携带权威会话版本并只会推进浏览器基线，避免单标签页首条请求错误进入同步冲突。
- 会话加载和切换期间安全处理暂时不存在的会话，不再读取未定义对象的 `messages`。

### Breaking Changes

- `PUT /sessions/{id}/snapshot` 现在要求请求携带服务端返回的 `_baseVersion`。未携带该字段的旧客户端会收到 `426 Upgrade Required`，以防止过期快照覆盖并发 Job 或其他标签页的更新。升级后必须刷新旧浏览器标签页；外部客户端必须先读取最新快照并在保存时回传 `_baseVersion`。

### Security

- Job 读取和按用户导出会显式核对持久化 owner；调用审计记录由后端统一生成并限制持久化字段长度。

### Compatibility

- Requires Halo `>=2.25.0`.
- Requires AI Foundation `>=1.0.0-beta.5`.
- 除会话快照的版本前置条件外，插件 ID、HTTP API、权限、Halo Extension GVK 和 ConfigMap 存储格式保持不变；无需数据迁移。
- `0.4.1-alpha.1` 是未发布的测试构建，必须使用唯一 JAR、校验和与预发布 Git 标签；不会替换 `0.3.5` 的历史制品。

## [0.3.5] - 2026-08-02

### Changed

- 将插件自有 Java 类从 `run.halo.aichatconsole` 迁移至 `io.github.joshuajj.haloaiconsole`，并同步更新端点导入与组件索引。

### Compatibility

- Requires Halo `>=2.25.0`.
- Requires AI Foundation `>=1.0.0-beta.5`.
- 插件 ID、HTTP API、权限、Halo Extension GVK 与 ConfigMap 存储格式保持不变；已有会话、消息、调用日志和图片缓存无需迁移。
- 从 `0.3.4` 热升级时，请在 Halo 插件页执行一次“重载插件”，或重启 Halo，以刷新包名迁移后的后端路由。
- 受 Halo 2.25.x 的 java-semver 解析限制，`plugin.yaml` 保持必装依赖 `ai-foundation: "*"`；插件启动校验器仍会严格拒绝缺失、无效或低于 `1.0.0-beta.5` 的 AI Foundation。

### Security

- 降低项目自有类与 Halo 官方或其他插件类发生命名空间冲突及维护方身份误解的风险。

### Privacy

- 未新增数据收集、第三方传输、遥测或外部资源。

## [0.3.4] - 2026-07-31

### Changed

- 将界面和用户提示中的通用技术术语恢复为 `Token` 和 `Job`，其他操作说明继续使用中文。

### Fixed

- 规避 Halo 2.25.x 对 AI Foundation 预发布依赖范围的解析缺陷，防止插件在安装阶段因 `LexerException` 失败。
- 新增启动期兼容性校验；AI Foundation 缺失、版本无效或低于 `1.0.0-beta.5` 时，插件会失败关闭并提供可操作的升级提示。

### Compatibility

- Requires Halo `>=2.25.0`.
- Requires AI Foundation `>=1.0.0-beta.5`.
- 受 Halo 当前 java-semver 解析限制，`plugin.yaml` 使用必装依赖 `ai-foundation: "*"`；实际最低版本由插件启动校验器强制执行。
- 未修改 API、存储格式、权限语义或模型调用逻辑。

### Privacy

- 未新增数据收集、第三方传输、遥测或外部资源。

## [0.3.3] - 2026-07-31

### Fixed

- 补充 Halo 应用市场要求的插件主页和问题反馈地址。
- 将插件安装后的默认启用状态改为关闭，由管理员确认配置后手动启用。

### Compatibility

- Requires Halo `>=2.25.0`.
- Requires AI Foundation `>=1.0.0-beta.5`.
- 未修改 API、存储格式、权限语义或模型调用逻辑。

### Privacy

- 未新增数据收集、第三方传输、遥测或外部资源。

## [0.3.2] - 2026-07-30

### Changed

- 将聊天操作、收藏筛选、模型类型、审计状态和个人数据操作提示统一为中文。
- 将面向用户的上传、配额、权限、上下文和图像生成错误改为清晰的中文提示。
- 将插件说明和管理员设置中的操作术语统一为中文。

### Compatibility

- Requires Halo `>=2.25.0`.
- Requires AI Foundation `>=1.0.0-beta.5`.
- 未修改 API 路径、存储格式、状态值或权限语义。

### Privacy

- 未新增数据收集、第三方传输、遥测或外部资源。

## [0.3.1] - 2026-07-29

### Changed

- 项目许可证改为 GNU AGPL v3。
- AI Foundation 强制依赖范围改为 `>=1.0.0-beta.5`。
- Favorite 操作增加图标。

### Compatibility

- Requires Halo `>=2.25.0`.
- Requires AI Foundation `>=1.0.0-beta.5`.

## [0.3.0] - 2026-07-28

### Changed

- 模型执行迁移到 AI Foundation Java SDK 的 `AiModelService`。
- 后台任务不再保存或重放用户浏览器 Cookie。
